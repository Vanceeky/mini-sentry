// This is a backend/API service, not a product UI (the Landing/Dashboard/Mobile
// apps are built separately) — this placeholder just confirms the service is up.
export default function HomePage() {
  return (
    <main>
      <h1>Mini Sentry API</h1>
      <p>
        This service exposes the Mini Sentry REST API. See <code>/docs/API.md</code> in the
        repository for the full contract.
      </p>
    </main>
  );
}
