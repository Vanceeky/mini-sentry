/**
 * Phase 0 placeholder export.
 *
 * This exists only to prove that the npm workspace, TypeScript build, and
 * demo import wiring work end to end. The real SDK API (init, capture,
 * transport, UI) is implemented starting in Phase 1.
 */
export function isSdkLoaded(): boolean {
  return true;
}
