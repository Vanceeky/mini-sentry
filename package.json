{
  "name": "mini-sentry",
  "version": "0.0.0",
  "private": true,
  "description": "Mini Sentry - lightweight client-side error monitoring SDK",
  "workspaces": [
    "sdk",
    "demo"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run build -w sdk && npm run typecheck --workspaces --if-present",
    "dev": "npm run dev -w demo"
  },
  "license": "ISC"
}
