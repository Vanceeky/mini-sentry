{
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal browser demo app for validating the Mini Sentry SDK locally",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vanceeq/canary": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.2.0"
  }
}
